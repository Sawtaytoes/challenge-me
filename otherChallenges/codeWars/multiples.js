const solution = maximum = (
  maximum  0
   0
   (
    Array(maximum)
    .fill(null)
    .map((
      value,
      index,
    ) = (
      index
    ))
    .filter((
      value,
    ) = (
      value !== 0
      && (
        value % 3 === 0
         value % 5 === 0
      )
    ))
    .reduce(
      (
        total,
        value,
      ) = (
        total + value
      ),
      0,
    )
  )
)
